- name: copy {{ component }} service
  ansible.builtin.copy:
   src: "{{ component }}.service"
   dest: /etc/yum.repos.d/{{ component }}.service

- name: daemon reload
  ansible.builtin.systemd:
   daemon_reload: yes
   state: restarted
   enabled: yes
   name: "{{ component }}"




